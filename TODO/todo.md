Issues & Vehicle Timeline

Issues should be resolvable — once resolved, they disappear from the main/active view
Keep a permanent audit trail of all issues/resolutions stored in the background
On the vehicle timeline, only show recent history by default (e.g., last month), with the ability to page back to older months

Branding

Fix logo text: should read "Sunny Fleet," not "Funny Fleet"
Re-upload/redo the logo — the PNG didn't render well — and improve overall branding

Profile Photos

Fix bug: uploading a custom photo sets it as "custom uploaded photo" but the app still blocks saving and asks to "enter a URL"
Remove all default/preset avatar images entirely

Dashboard Accuracy

"Vehicles in use" is working correctly and reflects real state — keep as-is
Inspection calendar does not reflect real data — needs to be fixed
"Today's Activity" shows random/placeholder data, and clicking into it shows mismatched info — needs to reflect actual state
Notifications badge statically shows "3 unread" — needs to reflect the real unread count

Equipment

Add a settings section to customize the equipment options list (currently fixed at 8 preset choices under "initial equipment – select all that apply")
Allow manager to add/edit/remove which options appear in that list
Still allow one-off custom equipment entries when adding a specific vehicle

Vehicle Page / Navigation

Rename the "Timeline" button to something clearer, since it actually contains the chronological timeline, assigned equipment, issues, and QR code tag — "Timeline" doesn't convey that

QR Code / Access Control

Fix: scanning a vehicle's QR code currently auto-signs the user in as the manager (Jacob) with full edit access
Employees scanning the QR code should only see a limited "employee page" — inspection to-do list (plus any future employee-facing features)

Inspections

Flagging an issue currently requires typing a detailed explanation every time
Add manager-configurable presets/canned reasons per item (e.g., "out of towels," "only 29 towels") as quick-select options, in addition to free text
Keep the current inspection question/answer format — it's working well

To-Do Lists

Let managers add to-do items/tasks
System should recognize if a vehicle was already inspected (e.g., in the morning) so it's not flagged as needed again
But support separate scheduled tasks (e.g., a night inspection) so intentional double inspections in a day still work

Reports

Current metrics: fleet inspection pass rate, total issues handled, active fleet size
Make reports customizable — let users add/choose different metrics to display

Date & Calendar

Fix current date bug — app shows the 18th when it's actually the 19th; needs to pull the real current date/time
Replace the static calendar image with an actual functional/interactive calendar

create mobile version so it's veyr phoen friendly
